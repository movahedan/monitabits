/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { RequestConfig, ResponseErrorConfig } from "../../mutator.server";
import fetch from "../../mutator.server";
import type {
	SessionsControllerGetCurrentSession400,
	SessionsControllerGetCurrentSessionQueryResponse,
} from "../types/SessionsControllerGetCurrentSession";

function getSessionsControllerGetCurrentSessionUrl() {
	const res = { method: "GET", url: "/api/sessions/current" as const };
	return res;
}

/**
 * @description Returns the current active or locked session status along with user statistics.If no active session exists, the session field will be null.Includes user stats like total time saved, current streak, and last relapse.
 * @summary Get current session status
 * {@link /api/sessions/current}
 */
export async function sessionsControllerGetCurrentSession(
	config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		SessionsControllerGetCurrentSessionQueryResponse,
		ResponseErrorConfig<SessionsControllerGetCurrentSession400>,
		unknown
	>({
		method: "GET",
		url: getSessionsControllerGetCurrentSessionUrl().url.toString(),
		...requestConfig,
	});
	return res.data;
}
