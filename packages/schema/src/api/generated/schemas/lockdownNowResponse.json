{
	"type": "object",
	"required": ["isLocked"],
	"properties": {
		"isLocked": {
			"type": "boolean",
			"description": "Whether the user is currently in a lockdown period"
		},
		"timeRemaining": {
			"type": "integer",
			"nullable": true,
			"description": "Time remaining in seconds (if locked)"
		},
		"sessionId": {
			"type": "string",
			"format": "uuid",
			"nullable": true,
			"description": "Current session ID (if locked)"
		}
	},
	"x-readme-ref-name": "LockdownNowResponse"
}
